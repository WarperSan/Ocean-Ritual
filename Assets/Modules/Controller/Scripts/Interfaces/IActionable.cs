namespace ControllerModule
{
    /// <summary>
    /// Defines the objects that can receive input actions
    /// </summary>
    public interface IActionable { /* This is only a flag to unify all the objects that wants to receive inputs */ }
}